# PK Tracker — Android · TODO

> רשימת עבודה חיה. ראה [`HANDOFF.md`](HANDOFF.md) למצב הכללי ו-[`CHANGELOG.md`](CHANGELOG.md)
> למה שכבר שוחרר. סמן `[x]` כשמסיימים.

עדכון אחרון: 2026-07-24

---

## 🔄 עכשיו בעבודה

- [ ] **סנכרון בין מכשירים (Firebase Firestore)** — התשתית המקומית מוכנה
      (uid גלובלי + מחיקה רכה, גרסה 2.2.0). נשאר לחבר את הענן:
  - [ ] להסיר את סיומת ה-`.debug` מ-`applicationId` (כדי שיתאים לחבילה
        `com.pktracker.android` הרשומה ב-Firebase). ⚠️ אחרי זה צריך להתקין
        מחדש פעם אחת (החבילה משתנה).
  - [ ] הזרקת `google-services.json` דרך **GitHub Secret** (הריפו ציבורי!)
        — המשתמש מוסיף secret בשם `GOOGLE_SERVICES_JSON` (base64), וה-CI
        מפענח אותו ל-`android/app/google-services.json` לפני הבנייה.
  - [ ] הוספת תלויות Firebase (BoM, firestore-ktx, auth-ktx) + פלאגין
        `com.google.gms.google-services`.
  - [ ] **התחברות אנונימית** (FirebaseAuth) — כל התקנה מקבלת uid משלה.
  - [ ] **שכבת סנכרון**: העלאת מנות שהשתנו (`changedSince`) ל-
        `users/{uid}/doses/{doseUid}`, האזנה (listener) לשינויים מרחוק,
        ומיזוג last-write-wins לפי `updatedAt` (מכבד מחיקות רכות).
        להוסיף אינדקס ייחודי על `uid` ב-Room ל-upsert נקי.
  - [ ] סנכרון הגדרות (אופציונלי) — או להשאיר הגדרות מקומיות.

## 🔜 הבא בתור

- [ ] **ווידג'ט למסך הבית** — מציג את הרמה הנוכחית + הפעולה הבאה, בצבע החומר.
      טקסט + ProgressBar (RemoteViews, בלי Compose/Canvas). עדכון תקופתי +
      ריענון אחרי רישום מנה. (הוסכם כ"חייבים".)
- [ ] **הרחבת התזכורות החכמות** מעבר לשתיים הקיימות:
  - [ ] תזכורת מבוססת-הרגל ("בד״כ אתה שותה כוס שנייה עכשיו") — מנתוני Insights.
  - [ ] "תזמון מושלם" — קפה עכשיו כדי לשיא ביעד שהגדרת.
  - [ ] חיזוק חיובי — "3 ימים מתחת לסף 👏".
  - [ ] הגדרת שעות שקטות מותאמות (כרגע קבוע 23:00–07:00).

## 💡 כדאי בהמשך (nice-to-have)

- [ ] **Google Sign-In** — זהות אמיתית לריבוי-משתמשים (מקשר את החשבון האנונימי,
      שומר נתונים אחרי החלפת מכשיר). כללי ה-Firestore כבר ערוכים per-user.
- [ ] צ'ק-אין "איך אתה מרגיש?" — לוג ערנות בפועל מול צפי, לכיול המודל למשתמש.
- [ ] שכבת-על של כמה חומרים בגרף (קפאין + ריטלין יחד).
- [ ] Quick-log — מהווידג'ט / פעולת התראה / קיצור long-press על האייקון.
- [ ] פריסת טאבלט דו-פאנלית (כרגע עמודה יחידה גם בטאבלט).
- [ ] נגישות מעמיקה (TalkBack מלא, יעדי מגע), פרופיילינג ביצועים.
- [ ] שפות נוספות.

## 📱 בדיקות מכשיר שצריך לעשות (עליך)

- [ ] מסלול זהב: רישום מנה → קריאת גרף → הגדרות → ייצוא CSV.
- [ ] החלפת חומרים — כל המסך מתלבש בצבע? הגרף/יחידות מתעדכנים?
- [ ] גרירת הגרף + חיצים (עבר/עתיד), הגדרת חלון-זמן.
- [ ] Insights עם נתונים אמיתיים (שעות שיא, ימים, רצף).
- [ ] תזכורות: להדליק בהגדרות, לאשר הרשאה, ולוודא שהתזכורת "קפה אחרון" קופצת
      בחלון הנכון (לפני חתך השינה) ולא בלילה.
- [ ] עברית/RTL בכל המסכים, החלפת שפה ושמירתה אחרי הפעלה מחדש.
- [ ] מקרי קצה: מנות סביב חצות, סיבוב מסך, היסטוריה גדולה, מחיקה + ביטול.
- [ ] מיגרציית DB: משתמש קיים עם נתונים → פתיחת 2.2.0 לא מאבדת מנות.

## 🚀 הפצה (עתידי)

- [ ] Google Play: הסרת סיומת debug, build של release חתום (keystore), AAB,
      Play Console, 20 בודקים ל-14 יום (חשבון אישי חדש), מדיניות פרטיות,
      Data Safety, דירוג תוכן. ⚠️ תוכן אלכוהול/בריאות → מבדק מדיניות קפדני.
- [ ] חלופות קלות: F-Droid, או להישאר עם GitHub Releases (סָייד-לוד, כמו היום).
